const User = require('./User');
const Expenses = require('./expenses');

// Expenses.hasOne(User, {
//     foreignKey:
//     onDelete: 
// });

// User.hasMany(Expenses, {
//     foreignKey: 
// });

module.exports = { User };